/**
 * Slot component — Phase 4 replaces this file with the real SequenceTimeline.
 */
export function OutreachTimeline() {
  return (
    <section className="rounded-lg border border-border bg-card p-4">
      <h2 className="text-sm font-medium">Outreach</h2>
      <p className="mt-2 text-sm leading-relaxed text-muted-foreground">
        No messages yet. Sequences with day offsets — and the halt-on-reply rule that keeps you
        from nudging someone who already answered — land here.
      </p>
      <p className="mt-2 font-mono text-xs text-faint">Outreach lands in Phase 4.</p>
    </section>
  )
}
